import { createTheme } from '@mui/material';

const theme = createTheme({
    palette: {
        primary: {
            main: '#00ADB5',
        },
        secondary: {
            main: '#393E46',
        },
        info: {
            main: '#222831',
        },
    },
});

export default theme;
